<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Preciso.Views"
             x:Class="Preciso.Views.EditarCadastroProfissionalView">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="15">
                <local:CadastroView />               

                <Button Style="{StaticResource StyleButton}"
                    Text="Editar"
                    Command="{Binding EditarDadosProfissionalCommand}" />

                <Button Style="{StaticResource StyleButton}"
                    Text="Cancelar" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>